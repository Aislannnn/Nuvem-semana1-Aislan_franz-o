# Att-Nuvem-23-02
site estático (HTML/CSS/JS) usando apenas o navegador, sem instalar Node, Git ou VS Code.

Erro 404 ([HTTP/2 404  0ms])
Foi feita a tentativa de atualizar o JavaScript e o Html, e após isso o problema foi com o próprio pages. 
